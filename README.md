## Neovim Setup

1. Clone Repo

2. Link Home Manager Config

```bash
mkdir -p ~/.config
ln -s <repo>/home-manager ~/.config/home-manager
```

3. Use Config

```bash
home-manager switch --impure
```

4. Use nvim

```bash
nvim
```
